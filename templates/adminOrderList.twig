{% include 'header.twig' %}
    {% include 'navbar.twig' %}
    
    {% include 'adminMenu.twig' with {'tab' : 3 } %}

    <div class="container-fluid content">

    <div class="container">
        <div class="table-responsive">
            <table class="table table-hover" id="myTable">
                <tr>
                    <th>#</th>
                    <th>Ambasador</th>
                    <th>Email</th>
                    <th>PESEL</th>
                    <th>Telefon</th>
                    <th>Rejestracja</th>
                </tr>
                {% for order in orders %}
                <tr>
                    <td>{{ user.ID }}</td>
                    <td>{{ user.CustomerName }}</td>
                    <td>{{ user.CustomerEmail }}</td>
                    <td>{{ user.CustomerPesel }}</td>
                    <td>{{ user.CustomerPhone }}</td>
                    <td>{{ user.Created }}</td>
                </tr>
                {% endfor %}
            </div>
        </table>
    </div><!-- /.container   -->

    </div>
{% include 'footer.twig' %}
